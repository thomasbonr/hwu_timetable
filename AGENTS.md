# Agent Instructions

- Use `python3` when documenting or running CLI commands.
- Run `pytest` before completing changes that affect Python code.
- Keep README usage examples in sync with the available CLI options.
