# HW Timetable Exporter

Command line utility to export Heriot-Watt University timetables to iCalendar.

## Usage

```bash
python -m hw_timetable.cli --offline --dump-json
```

See `python -m hw_timetable.cli -h` for options.
